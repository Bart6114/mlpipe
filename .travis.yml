language: python
python:
    - 2.7
    - 3.6
install:
    - pip install .[test]
    - pip install codecov
    - pip install pytest-cov
script:
    - py.test --cov=mlpipe tests/
after_success:
    - codecov
